---
phase: 06-filter-toolbar-redesign
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/dashboard/bar-chart/page.tsx
  - app/dashboard/box-plot/page.tsx
  - app/dashboard/bubbles/page.tsx
  - app/dashboard/candlestick/content.tsx
  - app/dashboard/chord/page.tsx
  - app/dashboard/correlation/content.tsx
  - app/dashboard/customers/page.tsx
  - app/dashboard/distribution/page.tsx
  - app/dashboard/explore/page.tsx
  - app/dashboard/financial-heatmap/page.tsx
  - app/dashboard/heatmap/page.tsx
  - app/dashboard/investor-network/page.tsx
  - app/dashboard/investor-stats/page.tsx
  - app/dashboard/investor-views/content.tsx
  - app/dashboard/landscape/page.tsx
  - app/dashboard/landscape-intro/page.tsx
  - app/dashboard/map/page.tsx
  - app/dashboard/marimekko/page.tsx
  - app/dashboard/maturity-matrix/content.tsx
  - app/dashboard/metros/page.tsx
  - app/dashboard/network/page.tsx
  - app/dashboard/parallel/page.tsx
  - app/dashboard/patterns/page.tsx
  - app/dashboard/periodic-table/page.tsx
  - app/dashboard/quadrant/page.tsx
  - app/dashboard/radar/page.tsx
  - app/dashboard/reports/content.tsx
  - app/dashboard/sankey/page.tsx
  - app/dashboard/slope/page.tsx
  - app/dashboard/spiral/page.tsx
  - app/dashboard/splom/page.tsx
  - app/dashboard/sunburst/page.tsx
  - app/dashboard/swot/content.tsx
  - app/dashboard/tab/advanced/page.tsx
  - app/dashboard/tab/financial/page.tsx
  - app/dashboard/tab/geographic/page.tsx
  - app/dashboard/tab/market/page.tsx
  - app/dashboard/tab/network/page.tsx
  - app/dashboard/timeline/page.tsx
  - app/dashboard/treemap/page.tsx
  - app/dashboard/wordcloud/page.tsx
  - components/dashboards/startup-dashboard.tsx
  - components/dashboards/vc-dashboard.tsx
  - components/dashboards/oem-dashboard.tsx
  - components/dashboards/isv-dashboard.tsx
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "No dashboard page renders VizFilterBar inline anymore"
    - "All chart pages still receive filtered data via useFilter context"
    - "Scenario dashboards no longer render VizFilterBar"
    - "The build passes after removing all VizFilterBar usages"
  artifacts:
    - path: "app/dashboard/bar-chart/page.tsx"
      provides: "Example of cleaned chart page (no VizFilterBar import/render)"
    - path: "components/dashboards/startup-dashboard.tsx"
      provides: "Example of cleaned scenario dashboard (no VizFilterBar)"
  key_links:
    - from: "app/dashboard/*/page.tsx"
      to: "contexts/filter-context.tsx"
      via: "useFilter() consumed through useThesisGatedData()"
      pattern: "useThesisGatedData"
---

<objective>
Remove VizFilterBar from all 45 dashboard page and content files, completing the migration to the shared FilterToolbar.

Purpose: With FilterProvider at layout level (Plan 01) and FilterToolbar in sidebar-shell (Plan 02), the per-page VizFilterBar is now redundant. Removing it from all 45 files eliminates the inline filter panel, reduces per-page bundle size, and ensures all filtering goes through the shared toolbar (UX-04). This is a mechanical bulk refactor but MUST be done carefully to avoid breaking any page.

Output: All 45 files cleaned of VizFilterBar imports and renders. Build passes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-filter-toolbar-redesign/06-RESEARCH.md
@.planning/phases/06-filter-toolbar-redesign/06-01-SUMMARY.md

<interfaces>
<!-- Pattern to follow for each file -->

BEFORE (typical chart page, e.g., bar-chart/page.tsx):
```typescript
import { VizFilterBar } from "@/components/viz-filter-bar"
// ...
function BarChartInner() {
  const { companies, filtered, isLoading } = useThesisGatedData()
  return (
    <div className="space-y-4">
      <div>
        <h1>...</h1>
        <p>...</p>
      </div>
      {isLoading ? <Skeleton /> : (
        <>
          <VizFilterBar companies={companies} />
          <BarChart data={filtered} />
        </>
      )}
    </div>
  )
}
```

AFTER (VizFilterBar removed):
```typescript
// REMOVED: import { VizFilterBar } from "@/components/viz-filter-bar"
// ...
function BarChartInner() {
  const { companies, filtered, isLoading } = useThesisGatedData()
  return (
    <div className="space-y-4">
      <div>
        <h1>...</h1>
        <p>...</p>
      </div>
      {isLoading ? <Skeleton /> : (
        <BarChart data={filtered} />
      )}
    </div>
  )
}
```

Key transformation rules:
1. Remove `import { VizFilterBar } from "@/components/viz-filter-bar"` line
2. Remove `<VizFilterBar companies={...} />` JSX element
3. If VizFilterBar was the only sibling in a Fragment (<>), remove the Fragment wrapper too
4. If `companies` from useThesisGatedData is no longer used after removing VizFilterBar, remove it from destructuring
5. Keep ALL other code intact -- charts, headings, loading states, etc.

BEFORE (scenario dashboard, e.g., startup-dashboard.tsx):
```typescript
import { VizFilterBar } from "@/components/viz-filter-bar"
// ...
<VizFilterBar companies={data} className="mb-4" />
```

AFTER:
```typescript
// REMOVED: import { VizFilterBar }
// REMOVED: <VizFilterBar companies={data} className="mb-4" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove VizFilterBar from all individual chart pages (38 files)</name>
  <files>
    app/dashboard/bar-chart/page.tsx, app/dashboard/box-plot/page.tsx, app/dashboard/bubbles/page.tsx,
    app/dashboard/candlestick/content.tsx, app/dashboard/chord/page.tsx, app/dashboard/correlation/content.tsx,
    app/dashboard/customers/page.tsx, app/dashboard/distribution/page.tsx, app/dashboard/explore/page.tsx,
    app/dashboard/financial-heatmap/page.tsx, app/dashboard/heatmap/page.tsx, app/dashboard/investor-network/page.tsx,
    app/dashboard/investor-stats/page.tsx, app/dashboard/investor-views/content.tsx, app/dashboard/landscape/page.tsx,
    app/dashboard/landscape-intro/page.tsx, app/dashboard/map/page.tsx, app/dashboard/marimekko/page.tsx,
    app/dashboard/maturity-matrix/content.tsx, app/dashboard/metros/page.tsx, app/dashboard/network/page.tsx,
    app/dashboard/parallel/page.tsx, app/dashboard/patterns/page.tsx, app/dashboard/periodic-table/page.tsx,
    app/dashboard/quadrant/page.tsx, app/dashboard/radar/page.tsx, app/dashboard/reports/content.tsx,
    app/dashboard/sankey/page.tsx, app/dashboard/slope/page.tsx, app/dashboard/spiral/page.tsx,
    app/dashboard/splom/page.tsx, app/dashboard/sunburst/page.tsx, app/dashboard/swot/content.tsx,
    app/dashboard/tab/advanced/page.tsx, app/dashboard/tab/financial/page.tsx, app/dashboard/tab/geographic/page.tsx,
    app/dashboard/tab/market/page.tsx, app/dashboard/tab/network/page.tsx,
    app/dashboard/timeline/page.tsx, app/dashboard/treemap/page.tsx, app/dashboard/wordcloud/page.tsx
  </files>
  <action>
    For EACH of the 41 chart page/content files listed above:

    1. Read the file
    2. Remove the `import { VizFilterBar } from "@/components/viz-filter-bar"` line
    3. Remove the `<VizFilterBar companies={...} />` or `<VizFilterBar companies={...} className={...} />` JSX element
    4. If the VizFilterBar was inside a Fragment (`<>...</>`), and removing it leaves only one child, remove the Fragment wrapper
    5. If `companies` is no longer referenced after removing VizFilterBar, remove it from the `useThesisGatedData()` destructuring (keep `filtered`, `isLoading`, and any other used vars)
    6. Save the file

    IMPORTANT: Read each file before editing. Some files have unique structures (content.tsx files with different layouts, tab pages with multiple charts, etc.). Do not blindly find-and-replace -- ensure the JSX structure remains valid after removal.

    Run `npx tsc --noEmit` after every 10 files to catch errors early. Fix any errors before proceeding.

    Expected pattern for most files: remove 2 lines (import + JSX). Some files may need Fragment cleanup (remove `<>` and `</>` wrappers if VizFilterBar was one of two children).
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 41 individual chart page/content files no longer import or render VizFilterBar. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove VizFilterBar from scenario dashboards and verify full build</name>
  <files>
    components/dashboards/startup-dashboard.tsx, components/dashboards/vc-dashboard.tsx,
    components/dashboards/oem-dashboard.tsx, components/dashboards/isv-dashboard.tsx
  </files>
  <action>
    1. For each of the 4 scenario dashboard files:
       - Read the file
       - Remove `import { VizFilterBar } from "@/components/viz-filter-bar"` line
       - Remove `<VizFilterBar companies={...} ... />` JSX element
       - If `data` or `companies` prop is no longer used for VizFilterBar but IS still used for charts/KPIs, keep it
       - Save the file

    2. Run full build: `npm run build`
       - This validates ALL 45 files compile correctly
       - This catches any missed VizFilterBar references

    3. Verify no remaining VizFilterBar imports exist outside of the component definition itself:
       - `grep -r "VizFilterBar" app/ components/dashboards/` should return 0 results
       - The only file that should still contain VizFilterBar is `components/viz-filter-bar.tsx` itself (keep it for now as deprecated reference)

    NOTE: Do NOT delete `components/viz-filter-bar.tsx` in this plan. It can be removed in a future cleanup pass once the toolbar is confirmed stable. Keeping it as deprecated reference is safe.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && npm run build 2>&1 | tail -10 && echo "---" && grep -r "VizFilterBar" app/ components/dashboards/ 2>&1 | wc -l</automated>
  </verify>
  <done>All 4 scenario dashboards cleaned. Full build passes. Zero VizFilterBar references remain in app/ or components/dashboards/. All filtering now goes through the shared toolbar (UX-04).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes -- full compilation succeeds with all 45 files cleaned
2. `grep -r "VizFilterBar" app/ components/dashboards/` returns 0 results
3. Manual: Visit /dashboard/bar-chart -- no inline filter panel, only the sticky toolbar at top
4. Manual: Visit /dashboard (scenario dashboard) -- no inline filter panel, only the sticky toolbar
5. Manual: Set a filter via toolbar on any chart page -- chart updates with filtered data (UX-04)
</verification>

<success_criteria>
- VizFilterBar removed from all 45 files (41 chart pages + 4 scenario dashboards)
- No VizFilterBar imports remain in app/ or components/dashboards/
- All charts still receive filtered data via useFilter context (UX-04)
- Full build passes with no errors
- viz-filter-bar.tsx kept as deprecated reference (not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-toolbar-redesign/06-03-SUMMARY.md`
</output>
