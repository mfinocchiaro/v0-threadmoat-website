---
phase: 04-transactional-email-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - emails/components/layout.tsx
  - emails/welcome.tsx
  - emails/receipt.tsx
  - emails/verification.tsx
  - emails/password-reset.tsx
  - lib/email.ts
autonomous: true
requirements: [EMAIL-03]

must_haves:
  truths:
    - "All email templates share a consistent ThreadMoat brand layout (dark background, violet accent, text logo, footer)"
    - "Existing verification and password-reset emails send using React Email components instead of raw HTML"
    - "Welcome and receipt email templates exist and accept typed props"
  artifacts:
    - path: "emails/components/layout.tsx"
      provides: "Shared branded email layout wrapper"
      exports: ["EmailLayout"]
    - path: "emails/welcome.tsx"
      provides: "Welcome email template"
      exports: ["WelcomeEmail"]
    - path: "emails/receipt.tsx"
      provides: "Payment receipt email template"
      exports: ["ReceiptEmail"]
    - path: "emails/verification.tsx"
      provides: "Verification email template (migrated from HTML)"
      exports: ["VerificationEmail"]
    - path: "emails/password-reset.tsx"
      provides: "Password reset email template (migrated from HTML)"
      exports: ["PasswordResetEmail"]
    - path: "lib/email.ts"
      provides: "Refactored email sender using React components + new sendWelcomeEmail/sendReceiptEmail exports"
      exports: ["sendVerificationEmail", "sendPasswordResetEmail", "sendWelcomeEmail", "sendReceiptEmail"]
  key_links:
    - from: "lib/email.ts"
      to: "emails/welcome.tsx"
      via: "import WelcomeEmail, passed to resend.emails.send({ react })"
      pattern: "react:\\s*WelcomeEmail"
    - from: "lib/email.ts"
      to: "emails/receipt.tsx"
      via: "import ReceiptEmail, passed to resend.emails.send({ react })"
      pattern: "react:\\s*ReceiptEmail"
    - from: "lib/email.ts"
      to: "emails/verification.tsx"
      via: "import VerificationEmail, replaces raw HTML"
      pattern: "react:\\s*VerificationEmail"
---

<objective>
Install @react-email/components, create branded email templates (layout, welcome, receipt, verification, password-reset), and refactor lib/email.ts to use React Email components via Resend's `react` param.

Purpose: Establish the email template system with ThreadMoat branding (EMAIL-03) and create all template components that Plan 02 will wire into the Stripe webhook.
Output: 5 React Email template files in emails/, refactored lib/email.ts with 4 send functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transactional-email-polish/04-RESEARCH.md
@lib/email.ts
@lib/products.ts

<interfaces>
<!-- Current email.ts exports that must be preserved (signature-compatible) -->
From lib/email.ts:
```typescript
export async function sendVerificationEmail(email: string, token: string): Promise<void>
export async function sendPasswordResetEmail(email: string, token: string): Promise<void>
```

<!-- New exports to add -->
```typescript
export async function sendWelcomeEmail(email: string, name: string | undefined, planName: string): Promise<void>
export async function sendReceiptEmail(email: string, name: string | undefined, amountFormatted: string, planName: string, periodStart: Date, periodEnd: Date, invoiceUrl: string): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependency and create branded email templates</name>
  <files>package.json, emails/components/layout.tsx, emails/welcome.tsx, emails/receipt.tsx, emails/verification.tsx, emails/password-reset.tsx</files>
  <read_first>
    - lib/email.ts (current raw HTML templates to migrate)
    - .planning/phases/04-transactional-email-polish/04-RESEARCH.md (brand colors, patterns, code examples)
  </read_first>
  <action>
1. Run `npm install @react-email/components` to add the dependency.

2. Create `emails/components/layout.tsx` — shared branded layout component (per EMAIL-03):
   - Export `EmailLayout` with props: `{ preview: string, children: React.ReactNode }`
   - Use `Html`, `Head`, `Preview`, `Body`, `Container`, `Section`, `Text` from `@react-email/components`
   - Brand styling: body background `#0a0a0a`, content card background `#171717` with `border: 1px solid #262626` and `border-radius: 8px`, padding `32px`
   - Header: "ThreadMoat" text in `#7c3aed` (violet), 24px bold, centered
   - Footer: "ThreadMoat Inc. - Market Intelligence for Industrial AI" in `#737373`, 12px, centered
   - All styles inline (no CSS classes — email clients strip style tags)

3. Create `emails/verification.tsx` — migrate from raw HTML in lib/email.ts:
   - Export `VerificationEmail` with props: `{ url: string }`
   - Wrap in `EmailLayout` with preview "Verify your ThreadMoat account"
   - Heading: "Verify Your Email" in `#e5e5e5`, 18px bold
   - Body text: "Click the button below to verify your email address" in `#a3a3a3`, 14px
   - CTA button: "Verify Email" with violet background `#7c3aed`, white text, 12px/24px padding, 6px border-radius
   - Expiry note: "This link expires in 24 hours..." in `#737373`, 12px

4. Create `emails/password-reset.tsx` — migrate from raw HTML:
   - Export `PasswordResetEmail` with props: `{ url: string }`
   - Same structure as verification but heading "Reset Your Password", button "Reset Password", expiry "1 hour"

5. Create `emails/welcome.tsx` — new template (per research code example):
   - Export `WelcomeEmail` with props: `{ name?: string, planName: string, dashboardUrl: string }`
   - Preview: "Welcome to ThreadMoat - your {planName} is active"
   - Heading: "Welcome to ThreadMoat{name ? `, ${name}` : ''}" in `#e5e5e5`
   - Body: subscription active message mentioning 44+ visualizations, 500+ startups
   - CTA: "Open Your Dashboard" button with violet background linking to dashboardUrl

6. Create `emails/receipt.tsx` — new template (per research code example):
   - Export `ReceiptEmail` with props: `{ name?: string, amountFormatted: string, planName: string, periodStart: string, periodEnd: string, invoiceUrl: string }`
   - Preview: "Payment receipt - {amountFormatted} for {planName}"
   - Heading: "Payment Receipt" in `#e5e5e5`
   - Body: "Your payment has been processed successfully." with optional name greeting
   - Line items: Amount, Plan, Period — each as `Text` with bold labels, `#e5e5e5` color
   - `Hr` separators with `borderColor: '#262626'`
   - CTA: "View Full Invoice" button with dark background `#262626`, light text, border `1px solid #404040`, linking to invoiceUrl
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && ls emails/components/layout.tsx emails/welcome.tsx emails/receipt.tsx emails/verification.tsx emails/password-reset.tsx && npx tsc --noEmit emails/components/layout.tsx emails/welcome.tsx emails/receipt.tsx emails/verification.tsx emails/password-reset.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "EmailLayout" emails/components/layout.tsx
    - grep -q "#7c3aed" emails/components/layout.tsx
    - grep -q "#0a0a0a" emails/components/layout.tsx
    - grep -q "WelcomeEmail" emails/welcome.tsx
    - grep -q "ReceiptEmail" emails/receipt.tsx
    - grep -q "VerificationEmail" emails/verification.tsx
    - grep -q "PasswordResetEmail" emails/password-reset.tsx
    - grep -q "invoiceUrl" emails/receipt.tsx
    - grep -q "dashboardUrl" emails/welcome.tsx
    - grep -q "@react-email/components" package.json
  </acceptance_criteria>
  <done>Six email template files exist in emails/ directory. All use @react-email/components imports, share the EmailLayout wrapper with ThreadMoat branding (violet accent, dark theme), and export named components with typed props. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor lib/email.ts to use React Email templates</name>
  <files>lib/email.ts</files>
  <read_first>
    - lib/email.ts (current implementation to refactor)
    - emails/components/layout.tsx (to understand import path)
    - emails/welcome.tsx (new template to integrate)
    - emails/receipt.tsx (new template to integrate)
    - emails/verification.tsx (migrated template)
    - emails/password-reset.tsx (migrated template)
  </read_first>
  <action>
Refactor `lib/email.ts` to replace raw HTML with React Email components:

1. Keep the existing `getResend()`, `FROM_EMAIL`, and `getBaseUrl()` helpers unchanged.

2. Refactor `sendVerificationEmail(email, token)`:
   - Import `VerificationEmail` from `@/emails/verification`
   - Build the verification URL the same way (getBaseUrl + /auth/verify-email?token=)
   - Use `react: VerificationEmail({ url })` instead of `html:` string
   - Keep the same error handling and console.log pattern

3. Refactor `sendPasswordResetEmail(email, token)`:
   - Import `PasswordResetEmail` from `@/emails/password-reset`
   - Build reset URL same way
   - Use `react: PasswordResetEmail({ url })` instead of `html:` string

4. Add `sendWelcomeEmail(email, name, planName)`:
   - Import `WelcomeEmail` from `@/emails/welcome`
   - Build dashboardUrl: `${getBaseUrl()}/dashboard`
   - Subject: "Welcome to ThreadMoat"
   - Use `react: WelcomeEmail({ name: name || undefined, planName, dashboardUrl })`
   - Same error handling pattern

5. Add `sendReceiptEmail(email, name, amountFormatted, planName, periodStart, periodEnd, invoiceUrl)`:
   - Import `ReceiptEmail` from `@/emails/receipt`
   - `periodStart` and `periodEnd` are Date objects; format them with `toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })`
   - Subject: `Payment Receipt - ${amountFormatted}`
   - Use `react: ReceiptEmail({ name: name || undefined, amountFormatted, planName, periodStart: formattedStart, periodEnd: formattedEnd, invoiceUrl })`

IMPORTANT: The `react` param in Resend's send() accepts the React element directly (e.g., `WelcomeEmail({ ...props })`). Do NOT use JSX syntax — call as a function. Do NOT import `render` from `@react-email/render`.
  </action>
  <verify>
    <automated>cd /Users/mfinocchiaro/Dropbox/Private/Obsidian/Projects/threadmoat-website && npx tsc --noEmit lib/email.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sendWelcomeEmail" lib/email.ts
    - grep -q "sendReceiptEmail" lib/email.ts
    - grep -q "react:" lib/email.ts
    - grep -qv "html:" lib/email.ts || echo "No raw html: params remain"
    - grep -q "@/emails/welcome" lib/email.ts
    - grep -q "@/emails/receipt" lib/email.ts
    - grep -q "@/emails/verification" lib/email.ts
    - grep -q "@/emails/password-reset" lib/email.ts
  </acceptance_criteria>
  <done>lib/email.ts exports 4 send functions (sendVerificationEmail, sendPasswordResetEmail, sendWelcomeEmail, sendReceiptEmail). All use `react:` param with React Email components instead of raw HTML strings. Existing function signatures for verification and password-reset are unchanged (backward compatible). TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (no import or type errors from new email files)
2. All 4 email template files exist and export named components
3. lib/email.ts has zero raw HTML strings remaining — all `html:` params replaced with `react:` params
4. grep confirms brand color `#7c3aed` appears in layout component
</verification>

<success_criteria>
- @react-email/components installed in package.json
- 5 template files in emails/ (layout + 4 email types)
- lib/email.ts exports sendWelcomeEmail and sendReceiptEmail (new) plus sendVerificationEmail and sendPasswordResetEmail (refactored)
- All templates use shared EmailLayout with ThreadMoat branding
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-transactional-email-polish/04-01-SUMMARY.md`
</output>
